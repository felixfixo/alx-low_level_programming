#include <stdio.h>

/**
 * _puts - function that prints a string,
 * followed by a new line, to stdout
 *
 * @str: String to be printed
 *
 * Return: Nothing
 */
void _puts(char *str)
{
printf("%s\n", str);
}
