#include <stdio.h>
#include "lists.h"

/**
 * main - function that returns key of username
 * keygen print a valid key for the username
 * Return: key of the username
 **/
int main()
{
  return (key);
}
